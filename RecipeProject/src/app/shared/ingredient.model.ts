export class Ingredient {
    // public name: string; 
    // public amount: number;
    //  Instead of writing the above lines we can write directly in constructor below
    constructor(public name: string, public amount: number) {
        // this.name = name;
        // this.amount = amount;
    }

}