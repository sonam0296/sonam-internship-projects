export interface LiveReloadCompilerOptions {
    projectName: string;
    tsServerConfigFile?: string;
    watchDir?: string;
    serverFilePath?: string;
    outputDir?: string;
    watchSsr?: boolean;
    serverBundlePath?: string;
    indexFilePath?: string;
    liveReloadOptions?: Record<string, any>;
    mainBundlePath?: string;
    browserBundlePath?: string;
}
export declare class LiveReloadCompiler {
    private readonly options;
    constructor(options?: LiveReloadCompilerOptions);
    run(): Promise<void>;
}
