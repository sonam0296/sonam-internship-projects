import { DynamicModule, OnModuleInit } from '@nestjs/common';
import { HttpAdapterHost } from '@nestjs/core';
import 'reflect-metadata';
import { AngularUniversalOptions } from './interfaces/angular-universal-options.interface';
export declare class AngularUniversalModule implements OnModuleInit {
    private readonly ngOptions;
    private readonly httpAdapterHost;
    constructor(ngOptions: AngularUniversalOptions, httpAdapterHost: HttpAdapterHost);
    static forRoot(options: AngularUniversalOptions): DynamicModule;
    onModuleInit(): Promise<void>;
}
